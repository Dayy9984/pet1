export default function Home() {
  return (
    <main className="page">
      <section className="baseline" aria-labelledby="baseline-title">
        <p className="eyebrow">Benchmark baseline</p>
        <h1 id="baseline-title">Ready for product implementation</h1>
        <p>
          This repository starts from a neutral Next.js shell. The harness must infer the product
          workflow from the user-provided service link, reference image, and design-system source.
        </p>
      </section>
    </main>
  );
}
